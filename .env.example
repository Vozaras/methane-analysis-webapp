# URL of the FastAPI backend exposing POST /predict
# Leave empty (or run without it) to use the built-in demo / mock detector.
METHANE_API_URL=http://localhost:8000

# Request timeout in seconds
METHANE_API_TIMEOUT=30
