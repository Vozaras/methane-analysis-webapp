# Placeholder theme (see DESIGN.md).
[theme]
base = "dark"
primaryColor = "#ebfc72"            # lime — sole accent
backgroundColor = "#13140e"         # page canvas
secondaryBackgroundColor = "#1b1c14"  # faint lift for sidebar / widgets
textColor = "#f4f3e8"               # primary text
font = "sans serif"

[server]
maxUploadSize = 50

[browser]
gatherUsageStats = false
