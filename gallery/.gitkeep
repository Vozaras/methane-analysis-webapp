# Keeps the gallery/ directory present in git and the Docker/Cloud Build context.
# Drop the 30 demo thumbnails here: s01_rgb.png … s15_ir.png (see gallery-data.js).
# Until they exist, the scene picker falls back to live ESRI satellite tiles.